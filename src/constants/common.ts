export const PATH = {
  HOME: "/",
  LOGIN: "/login",
  REGISTER: "/register",
  WORKPLACE: "/workplace",
};

export const PUBLIC_PAGES = [
  PATH.LOGIN,
  PATH.REGISTER,
  PATH.HOME,
  PATH.WORKPLACE,
];

export enum ENameCookie {
  ACCESS_TOKEN = "_next-auth.htshs",
}
